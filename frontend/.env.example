# OREON frontend — public runtime config
# Base URL of the FastAPI backend. The browser calls it directly (CORS is open).
# Local dev default:
VITE_API_URL=http://localhost:8000
# Production example:
# VITE_API_URL=https://api.your-domain.com
